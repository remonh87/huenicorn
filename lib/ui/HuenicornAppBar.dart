import 'package:flutter/material.dart';

class HuenicornAppBar extends AppBar {

  HuenicornAppBar(String title) : super(
      title: new Text(title),
      backgroundColor: Colors.grey[850],
  );
}
